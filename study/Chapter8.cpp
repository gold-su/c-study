#include <iostream>

using namespace std;
//식별자 = Identifier = 인식하다 
//메모리 주소를 프로그래머가 인식할수 있도록 바꿔준다.

int main(){  //main은 시스템이 찾는 첫번째 함수이다.
	
	int 3three(); //함수명은 숫자로 시작하면 안됨
	int . , ; , //등도 안된다.
	
	int value; a; b; //많이 사용함, 가급적이면 너무 길지 않으면 의미를 포함하는 뱐수명이 좋음.
	int num_apples; 나 n_apples 등
		n_a
		n_b //위와 같이 n_은 동일하게 가지고 가고 언더바 뒤로 차별을 두는 경우도 많다.
	//뭐든 코드는 적은게 좋다.
	변수명을 다 외울 필요는 없고, 하다보면 외워진다.
	꾸준히 하는게 중요. 
	//대문자는 변수명으로 잘 안쓴다. shift를 계속 눌러야한다. 번거롭다. 관습적으로 프로그래머들이 사용안한다.
	//유연성을 가지고 다른 이와 작업할때의 변수명을 따라가는게 좋다. 
	
		void MyFu~ //함수명은 대문자로 많이 시작한다
		//GetNumber 앞은 동사 뒤는 명사로 뭐하는 함수인지 작성하는 경우가 많다
		//요즘은 맨 앞을 소문자로 사용하기도 한다
		//동사명사일 필요가 꼭은 없지만 같이 작업하는 사람이 알아볼수 있어야 한다.
		// 또는 이름 옆에 주석으로 설명해준다.
		
		class MyClass{
			public:
			int m_variable; //class는 _를 맨 앞에 붙여서 작성하기도 함
		};
		
	return 0;
	
	int total; //정석
	int _orange; //상관없음 
	int VALUE; //모두 대문자면 작업할때 시간이 많이 들기 때문에 안됨
	int my variable name; //중간중간에 화이트스페이스가 들어가면 안됨
	int TotalCustomers; //너무 길기도 하고 상관은 없음
	int viod; //void는 int와 같은 코드에서 사용하는 단어기 때문에 안됨
	int numFruit; //문제없지만 작업하는 사람과 맞춰서 할 필요성있음
	int 2some; //숫자가 맨 앞에 있으면 안됨 
	int meters_of_pipe; //중간중간 언더바로 인해 시간이 많이 듬
	
	
	
	
}